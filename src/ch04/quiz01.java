package ch04;

public class quiz01 {
    // 1-1: 10 < x && x < 20;
    // 1-2: !(ch == ' ' || ch == '\t')
    // 1-3: ch == 'x' || ch == 'X'
    // 1-4: ch >= '0' && ch <= '9'
    // 1-5: (ch >= 'A' && ch <= 'Z') || (ch >= 'a' && ch <= 'z')
    // 1-6: !(year%400) || ( !(year%4) && (year%100) )
    // 1-7: !powerOn
    // 1-8: str.equals("yes")
}
