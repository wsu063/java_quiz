package ch06;

public class quiz01to11 {
    // 1. 3 // 하나의 클래스로 여러개의 객체를 생성할 수 있다.
    // 2. 4 // 클래스의 구성 멤버는 필드, 생성자, 메소드.
    // 3. 4 // 클래스는 필드와 메소드를 생략할 수 있고, 생성자를 반드시 가져야 한다. 없으면 자동생성
    // 4. 3 // 필드는 초기값을 주지 않으면 안된다?
    // 5. 1 // 객체를 만들면 생성자는 반드시 호출된다.
    // 6. 4 // 메소드의 이름은 중복해서 선언할 수 있다. 오버로딩
    // 7. 2 // 리턴 타입이 같아도 된다.
    // 8. 2 // 인스턴스 필드는 필드와 생성자에서 초기화 될 수 있다.
    // 9. 2 // 정적 블록에서 초기화 될 수 있다.
    // 10. 4
    // 11. 3 // default는 해당 패키지 내부에서만 사용을 허가한다.

}
